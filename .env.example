# Rush CMS API Configuration
# ==========================================

# Rush CMS API base URL (without trailing slash)
# Example: https://cms.example.com
NEXT_PUBLIC_API_URL=https://your-rush-cms-domain.com

# Rush CMS API Bearer Token (NEVER use NEXT_PUBLIC_ prefix - must be server-side only!)
# Generate this token in Rush CMS admin panel: Settings > API Tokens
# This token is tied to a specific site
API_TOKEN=your-sanctum-bearer-token-here

# Site ID from Rush CMS
# Find this in Rush CMS admin panel: Sites > Your Site > ID
SITE_ID=1

# Site Slug from Rush CMS (for API calls)
# Find this in Rush CMS admin panel: Sites > Your Site > Slug
SITE_SLUG=default

# Blog Collection ID (for fetching blog posts)
# Find this in Rush CMS admin panel: Collections > Blog > ID
BLOG_COLLECTION_ID=1

# Pages Collection ID (for dynamic pages)
# Find this in Rush CMS admin panel: Collections > Pages > ID
PAGES_COLLECTION_ID=2

# Navigation ID (for main menu)
# Find this in Rush CMS admin panel: Navigations > Main Navigation > ID
NAVIGATION_ID=1

# Contact Form Key (for contact page)
# Find this in Rush CMS admin panel: Forms > Contact Form > Key
CONTACT_FORM_KEY=contact

# Revalidation Configuration
# ==========================================

# Default ISR revalidation time in seconds
# Recommended values:
# - Institutional sites: 3600 (1 hour)
# - Blog: 1800 (30 minutes)
# - E-commerce/inventory: 300 (5 minutes)
# - Real-time content: 60 (1 minute)
REVALIDATE_TIME=1800

# Secret key for on-demand revalidation webhook
# Generate a random string (min 32 characters)
# Rush CMS will use this to authenticate revalidation requests
# Example: openssl rand -hex 32
REVALIDATE_SECRET=your-secret-key-for-webhook-revalidation

# Media Configuration
# ==========================================

# S3/DigitalOcean Spaces/CloudFront URL for media files
# Rush CMS stores media here, Next.js needs to know the domain for Image optimization
# Example: https://your-bucket.nyc3.digitaloceanspaces.com
NEXT_PUBLIC_S3_URL=https://your-bucket.s3.amazonaws.com

# S3 Bucket name (optional, for reference)
NEXT_PUBLIC_S3_BUCKET=rush-cms

# Site Configuration
# ==========================================

# Your site name (used in metadata)
NEXT_PUBLIC_SITE_NAME=My Site Name

# Your site URL (used for canonical URLs, sitemaps, Open Graph)
# Must include protocol (https://) and no trailing slash
NEXT_PUBLIC_SITE_URL=https://your-site.com

# Analytics (Optional)
# ==========================================

# Google Analytics 4 Measurement ID (optional)
# Example: G-XXXXXXXXXX
# Leave empty to disable Google Analytics
NEXT_PUBLIC_GA_ID=

# Plausible Analytics Domain (optional)
# Example: your-site.com
# Leave empty to disable Plausible
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Development Configuration
# ==========================================

# Node environment (automatically set by Next.js)
# development | production | test
NODE_ENV=development

# Enable Next.js debug mode (optional)
# Uncomment to see detailed build logs
# NEXT_DEBUG=1

# TypeScript type checking in build (optional)
# Set to false to skip type checking during build (faster builds)
# Not recommended for production
# NEXT_TYPE_CHECK=true
